# Afluencia360 API - Variables de entorno

# Puerto del servidor
PORT=3001

# URL del frontend (para CORS)
FRONTEND_URL=http://localhost:5173

# Firebase - Ruta al archivo de credenciales del service account
# Descarga desde: Firebase Console > Configuración > Cuentas de servicio > Generar nueva clave privada
FIREBASE_SERVICE_ACCOUNT_PATH=./config/serviceAccountKey.json

# Firebase Project ID (se obtiene del archivo de credenciales también)
FIREBASE_PROJECT_ID=tu-proyecto-firebase
